# Dependencies / build
node_modules
/build
.cache

# SECRETS — never commit. This is a key-handling tool: keep all keys local.
.env
.env.*
!.env.example
# Generated wallet files hold PRIVATE KEYS. The old `wallets.json` pattern did
# NOT match `wallets-1.json` etc. — ignore the whole dir AND every wallets-*.json.
mint-wallets/
wallets.json
wallets-*.json
*.key
*.keys.json

# Editor/agent scratch files
tmpclaude-*
